import Foundation

/// YOLOTermKit — Native macOS terminal emulation framework
/// 
/// This module provides the Track A (macOS) implementation of the YOLOTerm
/// shared contracts, wrapping SwiftTerm with the interface set defined in
/// contracts/interfaces.md.

public struct YOLOTermVersion {
    public static let version = "0.1.0-dev"
    public static let build = "M2"
}
